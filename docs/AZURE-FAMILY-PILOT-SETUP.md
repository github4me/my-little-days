# Archived: synthetic family setup

This guide is superseded by [Azure family sharing: setup and release](AZURE-FAMILY-SETUP.md), the current guide for complete history sharing, public customer admission, native activation and deletion processing.

Do not use the former static customer allowlist or bottle-feed-only activation instructions. Existing resource names, workflow filenames, SQL roles and the `family-pilot` GitHub environment remain unchanged for deployment compatibility; their names do not limit the release to synthetic data.

The current guide distinguishes implemented code from Azure, identity, database and device checks requiring actual release evidence. This notice does not assert deployment has occurred.
