// Keep optional purchases dormant while shipping the recording/family fixes.
// Re-enable only in a reviewed native release after Apple commerce and sandbox
// acceptance. This is deliberately not an environment or remote-config switch.
export const SUPPORT_PURCHASES_ENABLED = false;
